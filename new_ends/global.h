#ifndef MY_GLOBALS_H
#define MY_GLOBALS_H
#include <string>
using namespace std;

// This is a declaration of your variable, which tells the linker this value
// is found elsewhere.  Anyone who wishes to use it must include global.h,
// either directly or indirectly.
extern char* nameWifi;
extern char* passWifi;
extern char* botToken;
extern char* chatID;
extern string userScript;
extern string statementScript;


#endif